#include <bits/stdc++.h>
#define inf 0x3f3f3f3f
#define long long
using namespace std;

const int N = 1e5 + 5;

int T;

void solve(){
    int k, x, n;
    cin >> k >> x;
    cout << k * x + 1 << endl;
}

int main(){
    std::ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> T;
    while(T--){
        solve();
    }
    return 0;
}