# Solution: 构造字符串

容易将问题转化为若干个位置对应相等，若干位置不同

先将相同的合并在一起，然后不同的连边来处理不同的限制

如果同块连边，即无解

要求字典序最小，可以依次对于每个位置进行贪心

如果当前位置相同的块中已经确定了，直接取

否则找到所有要求不同的位置值的mex

直接实现上述做法，复杂度为$O(nm\alpha(n))$，这题放过暴力~~（所以数据也是乱造的~~

实际上可以通过倍增+并查集来实现合并，不同的关系只有$m$条，可以直接暴力处理贪心

ps:如果字符集大小有上限，我就不会写了。。
